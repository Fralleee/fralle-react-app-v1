// import sagaWrapper from 'store/sagas/sagaWrapper'
// import { PAYMENT } from 'store/actionTypes'
// import { all, takeLatest } from 'redux-saga/effects'

export default function* paymentWatcher() {
  // yield all([
  //   takeLatest(PAYMENT.GET.PAYMENTS_IN_BATCH.REQUEST, sagaWrapper(PAYMENT.GET.PAYMENTS_IN_BATCH.FAILURE, getBatchPayments)),
  //   takeLatest(PAYMENT.GET.BATCHES.REQUEST, sagaWrapper(PAYMENT.GET.BATCHES.FAILURE, getPaymentBatches))
  // ])
}
